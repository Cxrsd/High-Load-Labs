spring.application.name=redis-queue-lab

server.port=8080

# Redis connection — override via env vars REDIS_HOST, REDIS_PORT, REDIS_DB
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.database=${REDIS_DB:0}
